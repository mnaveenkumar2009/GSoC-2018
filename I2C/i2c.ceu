output on/off I2C;

var u8 rxBufferIndex=0;



output (on/off v) I2C do
    if v then
        rxBufferIndex = 0;
        rxBufferLength = 0;

        txBufferIndex = 0;
        txBufferLength = 0;

        twi_init();